<Window x:Class="MachineFlowers.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MachineFlowers.Views"
        mc:Ignorable="d"
        Title="Machine Flowers" Height="359" Width="426">
    <Grid>
        <Button Content="Select Image" Command="{Binding SelectImageCommand}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"/>
        <Image Source="{Binding ImageModel.ImagePath}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,55,0,0" Width="300" Height="200"/>
        <Label Content="{Binding ImageOutputLbl}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,270,0,0"/>
    </Grid>
</Window>
